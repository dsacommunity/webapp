import WorkInProgress from '@/components/WorkInProgress'

export default function Home() {
  return (
    <main className="bg-dark flex min-h-screen flex-col items-center justify-between">
      <WorkInProgress />
    </main>
  )
}
